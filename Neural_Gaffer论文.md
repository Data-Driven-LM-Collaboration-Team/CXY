# Neural Gaffer 论文总结

## （一）模型整体定位

提出 Neural Gaffer，一套二维扩散重光照模型+两阶段 NeRF
三维重光照流水线。

核心目标：输入单张物体图像+目标 HDR 环境光照，实现物理真实、细节丰富的图像/三维模型重光照；解决传统方案（纯SDS、单一LDR/HDR输入）存在的过曝、细节丢失、光照错乱等问题。

**整体链路：** 2D 重光照扩散模型（基础模块）→ 引导 3D NeRF 重光照（拓展应用）

## （二）扩散模型基础原理：从加噪到条件去噪

Neural Gaffer的核心底座是潜在扩散模型，因此在理解其重光照方法之前，需要先理解扩散模型的基本工作方式。扩散模型可以简单理解为一种“先破坏、再恢复”的生成模型：训练时，模型不断把干净图像加上高斯噪声，使图像逐渐变成随机噪声；生成时，模型则从随机噪声出发，逐步去除噪声，最终恢复出符合条件的清晰图像。

1.  前向扩散：把清晰图像逐步加噪

在训练阶段，首先从数据集中选取一张真实图像，然后随机选择一个时间步
t，并按照该时间步对应的噪声强度向图像中加入高斯噪声。

2.  反向扩散：让U-Net学会逐步去噪

扩散模型真正需要学习的是反向过程，也就是如何从带噪图像一步步恢复出干净图像。在Stable Diffusion一类模型中，核心网络通常是U-Net。U-Net的任务不是直接预测最终图像，而是预测当前带噪图像中包含的噪声。训练时，**系统知道真实加入的噪声，因此可以让
U-Net 输出预测噪声**，并用二者之间的误差作为损失函数。

![图1 扩散模型训练阶段的噪声预测示意图](media/image1.jpeg)

3.  为什么预测噪声就能生成图像？

扩散模型的关键思想在于：如果模型能够在每一个时间步准确预测噪声，那么就可以把随机噪声一步步还原成有意义的图像。生成阶段并没有真实图像作为答案，而是先随机采样一个噪声隐变量，然后反复调用U-Net预测噪声，并由调度器根据预测结果更新当前隐变量。经过多步迭代后，噪声成分不断减少，图像结构、纹理和语义信息逐渐出现，最终解码得到清晰图像。

![图2 扩散模型反向去噪生成过程示意图](media/image2.png)

4.  为什么要在 latent 隐空间中扩散？

如果直接在像素空间中对高分辨率图像反复加噪和去噪，计算量会非常大。Stable Diffusion 的改进之处在于先使用VAE编码器把图像压缩到低维 latent 隐空间，再在隐空间中进行扩散和去噪，最后用VAE解码器还原为像素图像。这样可以显著降低显存占用和计算复杂度，同时保留较强的图像生成能力。

![图3 Stable Diffusion / latent diffusion 整体生成流程示意图](media/image3.png)

5.  条件扩散：让模型按指定要求生成

扩散模型并不是只能随机生成图像，还可以加入条件信息来控制生成结果。普通文生图模型使用文本作为条件，图生图模型使用输入图像作为条件，而Neural Gaffer则进一步把“输入物体图像”和“目标环境光照”同时作为条件。这样，U-Net 在每一步去噪时都不是盲目生成，而是在回答一个更具体的问题：**如何在保留原物体身份的前提下，按照目标HDR环境光生成新的光照外观。**

因此，Neural Gaffer的重光照过程本质上可以理解为一种条件扩散生成过程：输入图像提供物体的形状、纹理和身份信息，目标环境贴图提供光照方向、颜色和强度信息，U-Net 在latent空间中逐步去噪，最终生成该物体在新光照条件下的图像。

## （三）模块一：二维重光照扩散模型（3.2 章节，核心底座）

### 1. 模型改造思路

二维重光照扩散模型是 Neural Gaffer 的核心底座。它并不是从零训练一个扩散模型，而是在预训练的潜在扩散模型基础上进行微调，并把原来的图像生成能力改造成“输入物体图像 + 目标环境光照 → 目标重光照图像”的条件生成能力。该模块的关键改造包括两点：一是将目标环境光处理为 LDR/HDR 双分支光照条件，二是将环境贴图旋转到相机坐标系下，使光照方向与图像坐标关系更加明确。

### 2. 完整工作流程

#### 2.1 光照预处理

输入目标 HDR 环境贴图 E 后，模型首先根据相机姿态旋转环境贴图，使环境光方向与当前图像的相机坐标系对齐，得到旋转后的环境贴图 Ê。这样做的目的是避免模型额外学习复杂的三维旋转关系，让“环境图中的亮区方向”和“图像中的受光方向”更容易对应起来。

随后，旋转后的环境贴图 Ê 被分成两个分支：

- **LDR 分支**：对 Ê 进行色调映射，得到 LDR 贴图 Ê_L，用于保留低亮度区域的环境细节和反射纹理。
- **HDR 分支**：对 Ê 进行对数映射和归一化，得到归一化 HDR 贴图 Ê_H，用于保留强光区域和整体物理能量信息。

这两个分支互相补充：LDR 分支更重视“环境细节”，HDR 分支更重视“光照强度与高光来源”。因此，双分支设计可以减少单独使用 LDR 或 HDR 时出现的细节丢失、高光不足或过曝问题。

#### 2.2 特征编码与条件拼接

完成光照预处理后，原始图像 x、LDR 光照贴图 Ê_L、HDR 光照贴图 Ê_H 会分别经过 VAE 编码器得到 latent 特征。随后，这些 latent 特征在通道维度上拼接，形成同时包含“物体内容信息”和“目标光照信息”的条件特征。

这里可以简单理解为：原始图像 x 告诉模型“要保持哪个物体”，LDR/HDR 光照条件告诉模型“这个物体应该被什么样的环境光照亮”。这些条件特征会在后续每一步 U-Net 去噪过程中持续参与计算。

#### 2.3 扩散模型训练与推理

训练阶段和推理阶段是两个不同过程，不能理解为同一个流程中同时发生的两条并行分支。训练阶段用于让 U-Net 学会在给定条件下预测噪声；推理阶段则使用已经训练好的 U-Net，从随机噪声出发生成重光照结果。

**训练阶段：学习条件噪声预测**

在训练阶段，模型并不是直接学习“原图到重光照图”的像素映射，而是采用扩散模型常用的噪声预测训练方式。具体来说，训练数据中已经有目标光照下的重光照图像 x̂_E。模型先将 x̂_E 编码到 latent 隐空间，然后随机选择一个时间步 t，并向该 latent 中加入对应强度的高斯噪声，得到带噪隐变量 z_t。

随后，U-Net 同时接收带噪隐变量 z_t、时间步 t、输入物体图像特征、LDR 光照特征、HDR 光照特征以及 CLIP 图像嵌入，并预测当前 z_t 中包含的噪声。由于训练时噪声是人为加入的，因此真实噪声 ε 是已知的，模型可以通过最小化预测噪声与真实噪声之间的 L2 误差来更新参数。

$$
L_{\mathrm{noise}}
= \left\| \epsilon - \epsilon_\theta\left(z_t, t, \widehat{E}, c(x)\right) \right\|_2^2
$$

其中，ε 表示真实加入的噪声，εθ 表示 U-Net 预测的噪声，z_t 表示第 t 个时间步的带噪隐变量，c(x) 表示输入图像 x 的 CLIP 图像嵌入。这个训练目标的本质是：让模型学会在“原图内容条件 + 目标光照条件”的共同约束下，判断当前 latent 中哪些成分是噪声、应该被去除。

**推理阶段：使用条件反向去噪生成重光照图像**

在推理阶段，模型不再需要目标重光照图像 x̂_E，也不会再计算训练损失。此时给定的是原始图像 x 和目标环境光照 E。模型先对原图和目标光照进行编码，得到内容条件与光照条件；同时从随机噪声 latent z_T 出发，利用训练好的 U-Net 进行多步反向去噪。

每一步去噪时，U-Net 都会参考当前 latent、时间步 t、原图条件和目标光照条件，预测当前 latent 中应该去除的噪声。经过多步迭代后，随机噪声逐渐被更新为干净 latent z_0。最后，z_0 经过 VAE 解码器还原为像素图像，得到最终的二维重光照结果 x̂_E。

因此，推理阶段可以概括为：

$$
z_T \xrightarrow[\text{condition: } x,\,E]{\text{U-Net 多步反向去噪}} z_0
\xrightarrow{\text{VAE Decoder}} \widehat{x}_E
$$

也就是说，Neural Gaffer 不是直接修改原图像素，而是把原图作为内容条件、把目标 HDR 环境贴图作为光照条件，引导扩散模型在 latent 空间中从随机噪声逐步生成目标光照下的新图像。

![图4 二维重光照扩散模型流程图](media/image4.png)

### 3. 模块价值

- 同时保留光照物理能量与画面细节，规避单一 LDR/HDR 输入的缺陷。
- 不需要显式分解 normal、BRDF、albedo 等中间物理量，而是通过条件扩散模型隐式学习几何、材质与光照之间的关系。
- 输出高质量二维重光照图像，并可作为后续 3D NeRF 重光照任务的视觉先验与参考约束。

## （四）模块二：三维 NeRF 重光照流水线（3.3 章节，应用拓展）

摒弃传统纯SDS方案，设计两阶段渐进优化流水线，全程冻结NeRF密度场（不改变物体形状），仅优化外观场。

### 阶段 1：粗粒度重光照（重建损失，定全局光照）

1.  利用二维扩散模型，为NeRF 所有视角生成标准重光照参考图；

2.  采用 L2 像素重建损失，约束NeRF 体渲染结果拟合参考图；

3.  输出结果：全局光照方向、强度基本正确，但画面模糊、存在轻微伪影。

原因：L2 损失天然偏好模糊解，且无细节先验约束。

### 阶段 2：细粒度细节优化（扩散引导损失，修细节）

针对阶段 1 结果偏模糊的问题，作者进一步使用扩散模型的先验进行细节精修。这个阶段的目的不是重新确定整体光照，而是在阶段 1 已经基本对齐全局光照的基础上，补充高光、纹理、材质质感等高频细节。

具体流程如下：

1. 随机采样一个相机视角，用阶段 1 的 NeRF 渲染得到当前视角下的模糊图像 x̂。

2. 将该图像送入 VAE 编码器，得到 latent 表示，并人为添加中等强度的标准高斯噪声，将其绑定到一个中间扩散时间步 t*。

   关键解释：阶段 1 图像中的模糊和伪影不是标准高斯噪声，扩散模型无法直接识别；人为加入标准噪声，是为了让该图像进入扩散模型熟悉的时间步体系，从而可以启动后续 DDIM 去噪流程。

3. 调用 DDIM 从中间时间步 t* 开始进行多步去噪，生成同视角下更清晰、更符合重光照先验的参考图像 x_relit(t*)。

4. 使用联合损失函数监督 NeRF 外观场优化：

$$
L_{\mathrm{diff}}
= w_1 \left\| \widehat{x} - x_{\mathrm{relit}}(t^*) \right\|_1
+ w_2 L_P\left(\widehat{x}, x_{\mathrm{relit}}(t^*)\right)
$$

其中，第一项是 L1 像素损失，用于保证色彩和亮度的全局一致；第二项是 LPIPS 感知损失，用于优化纹理、高光、材质等视觉细节与质感。

5. 迭代过程中，中间时间步 t* 随迭代指数衰减，即噪声强度逐步减弱，使优化过程从粗粒度修复逐渐过渡到细节打磨。

两阶段设计优势

1.  阶段1：快速对齐全局光照，规避SDS随机噪声、过曝问题；

2.  阶段2：借助扩散先验修复模糊、补充高频细节，提升画面真实感；

3.  整体效果远优于纯SDS、单阶段优化方案。

## （五）实验验证（核心结论）

### 1. 对比实验

- 二维重光照：对比DiLightNet、IC-Light，本文方法在高光、阴影、纹理还原上全面领先；

- 三维重光照：对比TensoIR、NVDIFFREC-MC，定量指标（PSNR/SSIM更高、LPIPS
  更低）与视觉效果均最优；

- 拓展任务：支持文本驱动重光照、物体场景融合，泛化能力强。

### 2. 消融实验（验证模块必要性）

1.  二维模块消融：

    - 取消环境贴图旋转 → 光照方向错乱；

    - 取消HDR分支 → 高光丢失；

    - 取消LDR分支 → 纹理细节丢失；

证明旋转对齐+双分支光照输入缺一不可。

2.  三维流水线消融：

    - 纯SDS：严重过曝、噪点多；

    - 缺少阶段1：整体光照完全错误；

    - 缺少阶段2：画面持续模糊；

证明两阶段流水线优于传统方案。
